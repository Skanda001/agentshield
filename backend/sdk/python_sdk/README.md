# AgentShield Python SDK

Wrap any Python function that an AI agent might call. Every call is
intercepted, sent to the AgentShield gateway for a verdict, and either
allowed, blocked, or escalated.

## Install

```bash
pip install -e sdk/python_sdk